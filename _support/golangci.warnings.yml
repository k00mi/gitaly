run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
  skip-dirs-use-default: true
  modules-download-mode: readonly

linters-settings:
  staticcheck:
    checks:
      - inherit

linters:
  disable-all: true
  enable:
    - staticcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
